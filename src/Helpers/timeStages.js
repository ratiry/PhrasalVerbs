import { timeStages } from "./Data"
import makeDate from "./MakingDate"
import datediff from "./calcDayDiff"
const setTimeStages=(timeInit,timeStage,wasExcercised)=>{
    if(timeStage==0){
        if(datediff(makeDate(),)){}
    }
}